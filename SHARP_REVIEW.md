# 🔥 网站锐评报告 & 改正清单

生成时间：2026-05-07  
评价者：AI 代码审查员

---

## ⚡ 严厉评价总结

### 现状评分：**68 / 100** ❌
- **美观度**：60 分（占位图重复、配色不够统一、某些页面布局松散）
- **功能完整度**：75 分（核心功能可用，但细节体验不佳）
- **代码质量**：72 分（有技术债、性能优化空间大）
- **用户体验**：65 分（响应式有问题、交互反馈不足）

### 核心问题（按严重程度）

---

## 🚨 第一级问题：严重缺陷（必须立即修复）

### 1️⃣ **EventsPage 数据统计虚假** ❌
**问题**：Hero 区显示的数据是静态的或错误的
```
"演出总数" 显示一个数字，但可能：
- 不是真实的事件总数
- 没有动态更新
- "时间范围"显示"整理中"（非常不专业）
```
**影响**：用户看到"整理中"会觉得网站是未完成的草稿。

**直接改正**：删除"时间范围：整理中"这一项。

---

### 2️⃣ **CultureMapPage 功能疑似缺失** ❌
**问题**：没看到地图的实际实现细节
- 地图展示是否正常？
- 标记点击是否有反应？
- 缩放/拖拽是否流畅？

**改正建议**：需要逐一验证。

---

### 3️⃣ **图片 Fallback 机制不完善** ❌
```javascript
// 当前代码
onError={(e) => {
  (e.currentTarget as HTMLImageElement).style.display = "none";
}}
```
**问题**：
- 图片加载失败就直接隐藏，卡片变丑
- 没有显示 Fallback 占位图
- 用户看不到有什么东西在这儿

**改正**：改为显示漂亮的占位图或填充色，不要让卡片坍塌。

---

### 4️⃣ **ReadingPage 推荐栏文案过时** ⚠️
```
"开放时间、安静程度、插座情况和预约要求均需以后续真实数据或官方页面为准。"
```
**问题**：
- 显示"后续真实数据"意味着当前数据是假的
- 这是向用户说"这个网站还没做好"
- 不专业

**改正**：改为"开放时间、服务情况请以官方公告为准。"

---

### 5️⃣ **EventsPage 加载更多按钮样式不一致** ⚠️
**问题**：
```
"加载更多（已显示 30 / 总数）"
```
- 用户不关心"已显示多少"，这是开发者的调试信息
- 按钮文案应该简洁："加载更多" 就够了

**改正**：改成简单的"加载更多"。

---

## 🎨 第二级问题：美观度问题（影响用户体验）

### 6️⃣ **卡片标签太多，显得拥挤** ⚠️
**现状**：某些资源卡片显示 4 个标签
```
[标签1] [标签2] [标签3] [标签4]
```
**问题**：
- 太多标签显得杂乱
- 用户无法快速理解核心特点

**改正**：卡片标签限制 3 个，按重要性排序。详情弹窗显示全部标签。

---

### 7️⃣ **ResourceCard 按钮太多且颜色混乱** ❌
**现状**：3 个按钮
```
[查看详情] [购票] [导航]
```
**问题**：
- 三个都是"主要按钮"，用户不知道点哪个
- 购票、导航不是每个资源都需要，但被同等对待

**改正**：
- "查看详情"保持突出（主按钮）
- "导航"次要（轻按钮）
- "购票"仅当 ticketUrl 存在时显示

---

### 8️⃣ **EventCard 设计不如 ResourceCard** ⚠️
**问题**：
- 列表布局不如卡片网格吸引人
- 可能缺少图片或视觉层次

**改正**：检查并改进 EventCard 的设计。

---

### 9️⃣ **AudienceRecommendationCard 图片高度不统一** ⚠️
**问题**：图片高度可能因为图片比例不同而显示不同
- 有的图片是正方形，有的是横向
- 卡片顶部参差不齐

**改正**：使用 `object-cover` 并固定高度，确保统一外观。

---

## ⚙️ 第三级问题：功能和体验

### 🔟 **搜索框在小屏幕上可能换行** ⚠️
**问题**：ResourceFilters 的 grid 布局在平板上可能崩溃
```
lg:grid-cols-[1.4fr_0.8fr_0.8fr_0.8fr_0.7fr_0.7fr_auto]
```
**改正**：添加 md 和 sm 的响应式布局。

---

### 1️⃣1️⃣ **EventDetailModal 缺少优化** ⚠️
**问题**：
- 没看到是否有优化的样式
- 是否显示门票链接？
- 是否显示高德导航？

**改正**：需要检查和完善。

---

### 1️⃣2️⃣ **首页 WeeklyCulture / HomeEvents 可能重复显示** ⚠️
**问题**：
- HomePage 同时显示 WeeklyCulture 和 HomeEvents
- 这两个是否显示同一批事件？
- 如果是，为什么要显示两次？

**改正**：检查是否需要合并或区分。

---

## 🐛 第四级问题：代码质量

### 1️⃣3️⃣ **没有使用 displayResources 优化** ❌
**问题**：
- ResourcesPage 虽然有排序逻辑，但没有用上 `generateDisplayResources()`
- 没有充分利用清洁数据层

**改正**：考虑集成 displayResources 以简化代码。

---

### 1️⃣4️⃣ **类型定义不够严格** ⚠️
**问题**：
- 某些地方用 `any` 类型
- AudienceRecommendationCard 参数类型松散

**改正**：改为严格的 TypeScript 类型。

---

### 1️⃣5️⃣ **EventCard 和 ResourceCard 重复代码** ⚠️
**问题**：两个卡片组件有相似的图片处理、按钮逻辑
**改正**：考虑提取公共组件。

---

## 📱 第五级问题：响应式设计

### 1️⃣6️⃣ **手机端页面间距可能不合理** ⚠️
**问题**：
- 某些页面 padding 固定，在小屏幕上可能过大或过小
- 栅栏布局可能在手机上显示不当

**改正**：在手机上测试每个页面。

---

### 1️⃣7️⃣ **Sidebar 在小屏幕上不显示** ⚠️
**问题**：
- ResourcesPage 右侧栏在小屏幕上隐藏
- 用户在手机上看不到"精选推荐"

**改正**：手机端应该把右侧栏放在底部，而不是完全隐藏。

---

## 📊 最严重的 3 个问题（按优先级）

| 排序 | 问题 | 严重度 | 修复时间 |
|-----|------|--------|---------|
| **#1** | EventsPage "时间范围：整理中" 太不专业 | 🔴 严重 | 5 分钟 |
| **#2** | 图片加载失败导致卡片坍塌 | 🔴 严重 | 15 分钟 |
| **#3** | 卡片按钮太多且颜色混乱 | 🟠 中等 | 20 分钟 |

---

## ✅ 改正行动计划

我现在直接改正这些问题。预计修复：
- ✅ EventsPage 数据统计
- ✅ 图片 Fallback 机制
- ✅ 按钮逻辑和样式
- ✅ 文案优化
- ✅ 响应式改进
- ✅ 类型安全性

---

